apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: keystone-py
  name: keystone-py
spec:
  ports:
    - name: http
      port: 5000
      protocol: TCP
      targetPort: 5000
  selector:
    app.kubernetes.io/name: keystone-py
